import { useState } from 'react';
export function DropSignup() {
 const [email,setEmail]=useState(''),[books,setBooks]=useState(true),[merch,setMerch]=useState(true),[consent,setConsent]=useState(false),[status,setStatus]=useState(''),[busy,setBusy]=useState(false);
 async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setStatus('');try{const r=await fetch('/api/subscriptions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,consent,interests:[...(books?['books']:[]),...(merch?['merchandise']:[])],source:new URLSearchParams(location.search).get('utm_source')||'website'})});const d=await r.json();if(!r.ok)throw new Error(d.error);setStatus('Your drop-list request has been received. Thank you.');}catch(e){setStatus(e instanceof Error?e.message:'Please try again.');}finally{setBusy(false);}}
 return <section id="drops" className="public-section"><div className="public-section-header"><span className="signal">The story continues here</span><h2>Get the next OPAIJA drop.</h2><p>Book releases and merchandise announcements, direct from OPAIJA. Choose what you want to hear about.</p></div><form onSubmit={submit} style={{maxWidth:560,display:'grid',gap:16}}><label>Email address<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)} style={{display:'block',width:'100%',padding:14}}/></label><div><label><input type="checkbox" checked={books} onChange={e=>setBooks(e.target.checked)}/> Books </label><label><input type="checkbox" checked={merch} onChange={e=>setMerch(e.target.checked)}/> Merchandise</label></div><label><input type="checkbox" required checked={consent} onChange={e=>setConsent(e.target.checked)}/> Email me about my selected OPAIJA drops. I can unsubscribe at any time.</label><button className="primary-action" disabled={busy||(!books&&!merch)}>{busy?'Saving…':'Join the drop list'}</button><p role="status">{status}</p><small>Free updates. Joining does not purchase a product or start a paid subscription.</small></form></section>;
}
